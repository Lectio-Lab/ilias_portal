{
  "mcpServers": {
    "ilias-portal": {
      "command": "/bin/bash",
      "args": ["{{KIT_HOME}}/agent/scripts/start-mcp.sh"],
      "env": {
        "ILIAS_PORTAL_HOME": "{{KIT_HOME}}"
      }
    }
  }
}

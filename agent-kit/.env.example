SECRET_KEY=dev-secret-key-change-in-production
DEBUG=True
DB_NAME=ilias_portal
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5434

# Use installed Chrome for MFA (set by install.sh on macOS when Chrome is present)
# PLAYWRIGHT_BROWSER_CHANNEL=chrome
BACKEND_PORT=8010
